{{!< default}}
{{!-- The tag above means: insert everything in this file
into the {body} of the default.hbs template --}}
{{!-- Chamada Hackerrank --}}
{{#is "home"}}
    <article id="banner">
        <div class="content">
            <header>
                <h1><a href="http://bit.ly/AumenteSuaChanceNaEntrevista">Aumente em até 70% sua chance de passar na vaga de Programadora Junior</a></h1>
            </header>
            <ul class="actions">
                <li><a href="http://bit.ly/AumenteSuaChanceNaEntrevista" class="button big">Saiba mais</a></li>
            </ul>
        </div>
    </article>

{{/is}}

{{!-- Posts --}}
<section>

    <header class="major">
        <h2>Posts Recentes</h2>
    </header>

    <section class="posts">
        {{#foreach posts}}
        <article>
            {{#if feature_image}}
            <a href="{{url}}" class="image fit"><img src="{{img_url feature_image}}" alt="{{title}}" /></a>
            {{/if}}
            <h3><a href="{{url}}">{{title}}</a></h3>
            <p>{{excerpt words="35"}}</p>
            <ul class="actions special">
                <li><a href="{{url}}" class="button">Ir para o Post</a></li>
            </ul>
        </article>
        {{/foreach}}
    </section>

</section>

{{!-- Pagination --}}
{{pagination}}

