<footer>
    <nav class="pagination" role="navigation">
        {{#if prev}}
        <a class="page previous newer-posts" href="{{page_url prev}}">Mais recentes</a>
        {{/if}}
        <span class="page-number">Página {{page}} de {{pages}}</span>
        {{#if next}}
        <a class="page next older-posts" href="{{page_url next}}">Mais antigos</a>
        {{/if}}
    </nav>
</footer>